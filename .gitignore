# API 키 — 절대 올리지 않는다
.env
.env.*
!.env.example

# Streamlit Secrets(관리자 비밀번호·API 키) — 절대 올리지 않는다
.streamlit/secrets.toml

# 앱이 만들어내는 것
data/
images/
__pycache__/
*.pyc

# 실제 기관 제출 엑셀(개인정보 포함) — 절대 올리지 않는다
sample/
