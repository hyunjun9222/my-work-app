# 외부 접속 방법

이 앱은 기본값이 **이 PC 에서만 접속**(127.0.0.1)이다. 밖에서 들어오게 하려면 아래 순서를 따른다.

## 1. 서버를 외부 접속 모드로 켠다

```powershell
python app.py --public              # 8000번 포트, 모든 랜카드에서 수신
python app.py 9000 --public         # 포트 지정
python app.py --public --no-browser # 브라우저 자동 실행 없이 (서버 PC 에 두고 쓸 때)
```

켜면 콘솔에 같은 망에서 쓸 주소(`http://192.168.x.x:8000`)가 찍힌다.

## 2. 어디까지 열지 고른다

| 범위 | 방법 | 준비물 |
|---|---|---|
| 같은 사무실(사내망) | 콘솔에 찍힌 `http://내부IP:포트` 를 그대로 알려준다 | Windows 방화벽에서 해당 포트 허용 |
| 인터넷(임시·시연) | 터널 프로그램으로 임시 주소를 만든다 | cloudflared 또는 ngrok |
| 인터넷(상시) | 공유기 포트포워딩 + 고정 IP·도메인 | 사내 IT 협의, HTTPS 인증서 |

### 방화벽 허용 (같은 망)

```powershell
New-NetFirewallRule -DisplayName "훈련실적취합 8000" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow
```

### 터널 (인터넷, 가장 간단)

```powershell
# Cloudflare Tunnel — 설치 후
cloudflared tunnel --url http://localhost:8000
# 또는 ngrok
ngrok http 8000
```

둘 다 `https://...` 주소를 만들어 주므로 비밀번호가 암호화되어 오간다. 주소를 아는 사람은 누구나 로그인 화면까지 닿으니, 계정을 나눠 준 사람에게만 주소를 알린다.

## 3. 반드시 지킬 것

- **HTTPS 없이 인터넷에 열지 않는다.** 이 앱은 HTTP 라 그대로 열면 아이디·비밀번호가 평문으로 지나간다. 터널이나 리버스 프록시(nginx·IIS)를 앞에 둔다.
- **관리자 임시 비밀번호를 먼저 바꾼다.** 첫 실행 때 콘솔에 찍힌 비밀번호를 그대로 두고 열지 않는다.
- **기관 계정은 기관별로 발급한다.** 계정 공유는 누가 무엇을 냈는지 추적을 없앤다.
- **`data/` 백업.** 제출·계정이 모두 이 폴더의 JSON 이다. 외부 접속을 열기 전에 복사본을 둔다.
- **로그인 시도 제한**이 IP 당 10분에 10회로 걸려 있다. 프록시 뒤에서는 `X-Forwarded-For` 를 보고 판단한다.

## 4. 세션 주의

세션은 메모리에 있어 **서버를 재시작하면 전원 로그아웃**된다. 외부 사용자가 붙어 있는 동안에는 서버를 껐다 켜지 않는다.
